# Outreach auto-import inbox

Drop CSVs in `inbox/` using the exact import contract
(header: day,lane,company,contact_name,title,email,website,city,state,segment,project_signal,source_url; lane=verified).
The 19:00 Phoenix job imports them automatically. Never delete files; imports are idempotent.
