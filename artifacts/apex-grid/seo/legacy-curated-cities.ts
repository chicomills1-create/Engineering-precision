/** Fixed inventory of curated cities that existed before sourced promotion
 * metadata became mandatory. Never add new cities here; migrate entries out by
 * adding approved research metadata to their CityData record. */
export const LEGACY_CURATED_CITY_KEYS = new Set([
]);